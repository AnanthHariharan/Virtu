# Supabase — from your project's API settings.
# The anon key is safe in the client because Row Level Security is on
# and every policy is scoped to auth.uid().
NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOi...

# Extraction worker (Phase 2 — not used by the app yet).
# Points at whatever speaks the OpenAI chat-completions shape:
#   LM Studio      http://localhost:1234/v1
#   mlx_lm.server  http://localhost:8080/v1
#   Anthropic      via a small proxy, or swap the client
LLM_BASE_URL=http://localhost:1234/v1
LLM_API_KEY=not-needed-locally
LLM_MODEL=qwen3-8b-instruct-4bit
